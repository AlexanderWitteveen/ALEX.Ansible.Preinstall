---

- name: "Playbook: Preinstall 1 - prep for ansible"
  hosts: "{{ args_host }}"
  gather_facts: false
  become: false

  vars_files:
    - /etc/ansible/inventory/vars.yml

  roles:
    - alex.preinstall.preinstall1
